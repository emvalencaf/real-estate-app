import * as Styled from "./styles";

// components
import Heading from "../../components/Heading";

const Home = () => {
	return (
		<Styled.Wrapper>
			<Heading>olá</Heading>
		</Styled.Wrapper>
	);
};

export default Home;
